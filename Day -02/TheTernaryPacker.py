#Have a variable score = 85. Assign a variable ststus to "Pass" if score > 70 else "Fail".
#Solve this in exactly one line of code

score = 85

status = "Pass" if score > 70 else "Fail"

print(status)
